title: Enablis Policies
description: A home for Enablis policies and associated documentation
theme: minima # This is a simple default theme
baseurl: "" # Leave empty for username.github.io or use "/repo-name" for project sites
url: "https://enablis-co.github.io" # Replace with your GitHub Pages URL

# Build settings
markdown: kramdown
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-search

# Add these lines to your existing config
include:
  - Policies
  - Procedures
  - Guides

# Optional: Set default layouts for files in these directories
defaults:
  - scope:
      path: "Policies"
      type: "pages"
    values:
      layout: "page"
  - scope:
      path: "Procedures"
      type: "pages"
    values:
      layout: "page"
  - scope:
      path: "Guides"
      type: "pages"
    values:
      layout: "page" 